# Scribble AI - Hand Gesture Game (Server Dependencies)
# Install with: pip install -r requirements.txt

# ── Server ──────────────────────────────────
flask>=2.3.0
flask-socketio>=5.3.0
python-socketio>=5.8.0
eventlet>=0.33.0

# NOTE: opencv-python, mediapipe, scikit-learn, etc.
# are only needed for the local Python script (hand_gesture_draw.py).
# Install them locally with: pip install opencv-python mediapipe scikit-learn
